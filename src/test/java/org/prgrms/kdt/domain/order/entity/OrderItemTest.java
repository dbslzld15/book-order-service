package org.prgrms.kdt.domain.order.entity;

import org.junit.jupiter.api.Test;
import org.prgrms.kdt.domain.book.vo.Price;

import java.time.LocalDateTime;

import static java.time.LocalDateTime.now;
import static org.junit.jupiter.api.Assertions.*;

class OrderItemTest {

    @Test
    public void asd() throws Exception {
        //given
//        OrderItem orderItem = OrderItem.builder().orderQuantity(-1).build();
//        OrderItem orderItem1 = new OrderItem(now(), now(), 1L, new Price(1000L), -1, 1L, 1L);
        //when

        //then
    }
}