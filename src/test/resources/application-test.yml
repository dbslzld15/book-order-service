spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/book
    username: sa
    password:
    driver-class-name: org.h2.Driver
  sql:
    init:
      schema-locations:
        - classpath*:db/schema.sql
      mode: always
  h2:
    console:
      enabled: true